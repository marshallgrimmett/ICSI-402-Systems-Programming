#include <stdio.h>

//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
int main(int argc, char *argv[]) {
  if (argc != 1 && argc != 2) {
    printf("Invalid number of arguments.");
    return -1;
  }
  // TODO
  return 0;
}
